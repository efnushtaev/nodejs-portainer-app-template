FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Копируем конфиг Nginx в том
RUN mkdir -p /nginx-config
COPY ../nginx/nginx.conf /nginx-config/