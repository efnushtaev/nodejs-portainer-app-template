# Исправляем регистр первой строки
FROM node:18-alpine AS build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Убираем nginx из клиентского образа
FROM nginx:alpine
COPY --from=build /app/build /usr/share/nginx/html